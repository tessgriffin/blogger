<h1>Tags Index</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'tag_name' %>
      <%= link_to "delete", tag_path(tag), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to "<< to articles", articles_path %>